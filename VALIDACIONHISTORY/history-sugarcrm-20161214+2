    1  history 
    2  #VER LA VERSION DE SHELL QUE TENGO
    3  echo $SHELL
    4  echo $BASH_VERSION
    5  echo $BASH_VERSION | head
    6  echo $BASH_VERSION | cut -d "(" -f1
    7  #EN EL COMANDO ANTERIOR CORTAMOS LA SALIDA ESTANDAR DEFINIENDO "(" COMO DELIMITADOR
    8  cd .
    9  cd ..
   10  ls
   11  cd src/
   12  ls
   13  cd ..
   14  ls
   15  cd Proyecto
   16  cd src/
   17  ls
   18  #PROCEDEMOS A CAMBIAR LOS PERMISOS DE LECTURA
   19  ls
   20  ./exit-codes.sh 
   21  hmod 750 exit-codes.sh 
   22  chmod 750 exit-codes.sh 
   23  ls -l
   24  ./exit-codes.sh 
   25  echo $?
   26  vim exit-codes.sh 
   27  #A PESAR DE EXISTIR UN ERROR EVIDENTE , AL FINAL DEL CAMOANDO SE LE ESPECIFICA QUE ME RETORNE UN 0 COMO CONCLUSION AFIRMATIVA DEL COMANDO
   28  vim variables-01.sh 
   29  ls
   30  chmod 750 resumen.sh 
   31  ls
   32  /resumen.sh 
   33  chmod 750  *.sh
   34  ls
   35  ./resumen.sh 
   36  echo $BASH_VERSION | cut -d "(" -f1` == $VERSION_BASH
   37  echo $BASH_VERSION | cut -d "(" -f1` == $VERSION_BASH 
   38  echo $BASH_VERSION echo $BASH_VERSION 
   39  echo $BASH_VERSION | cut -d "(" -f1`
   40  echo $BASH_VERSION | cut -d "(" -f1` == $VERSION_BASH || 
   41  `echo $BASH_VERSION | cut -d "(" -f1` == $VERSION_BASH ||  `lsb_release -i  | awk '{print $3}'` == Ubuntu 
   42  lsb_release -i
   43  ./resumen.sh cosa1 cosa2 cosa3
   44  lsb_release -r
   45  lsb_release
   46  cd
   47  lsb_release
   48  lsb_release -i  | awk '{print $3}'
   49  lsb_release -rs
   50  ç
   51  echo $BASH_VERSION 
   52  #EL SCRIPT resumen.sh, me indica si la version y la distribucion son las que tengo en mi maquina , indcar que es compatible, y validar si laca antidad de parametros ingresados es correcta
   53  cd Proyecto-de-Programacion/
   54  cd src/
   55  ls
   56  ./distros.sh 
   57  #AL UTILIZAR UN CENTOS INDICA QUE NO ES SOPORTADA
   58  history > VALIDACIONHISTORY/history-$HOSTNAME-`date +%Y%m%d`+3
   59  cd ..
   60  history > VALIDACIONHISTORY/history-$HOSTNAME-`date +%Y%m%d`+2
